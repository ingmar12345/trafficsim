namespace Assets
{
    /// <summary>
    /// Enum to indicate the state the train crossing is currently in.
    /// </summary>
    public enum TrainCrossState
    {
        Open,
        Opening,
        Closing,
        Closed,
        StartOpening,
        StartClosing
    }
}
